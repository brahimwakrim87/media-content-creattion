# PRODUCTION ENVIRONMENT — Copy to .env.prod and fill in real values
# Generate secrets: openssl rand -hex 32

APP_ENV=prod
APP_SECRET=<generate-with-openssl-rand-hex-32>
JWT_SECRET_KEY=/var/www/backend/config/jwt/private.pem
JWT_PUBLIC_KEY=/var/www/backend/config/jwt/public.pem
JWT_PASSPHRASE=<generate-with-openssl-rand-hex-32>

DATABASE_URL="postgresql://dmcc_app:<POSTGRES_PASSWORD>@postgres:5432/dmcc_db?serverVersion=16&charset=utf8"
POSTGRES_PASSWORD=<strong-password>

REDIS_URL=redis://redis:6379
MESSENGER_TRANSPORT_DSN=redis://redis:6379/messages

CORS_ALLOW_ORIGIN='^https://mmc\.ilinqsoft\.com$'

N8N_WEBHOOK_BASE_URL=http://n8n:5678/webhook
WEBHOOK_SECRET=<generate-with-openssl-rand-hex-32>
MAKE_WEBHOOK_SECRET=<generate-with-openssl-rand-hex-32>

N8N_AUTH_USER=admin
N8N_AUTH_PASSWORD=<strong-password>
N8N_ENCRYPTION_KEY=<generate-with-openssl-rand-hex-32>

ANTHROPIC_API_KEY=
OAUTH_FACEBOOK_CLIENT_ID=
OAUTH_FACEBOOK_CLIENT_SECRET=
OAUTH_GOOGLE_CLIENT_ID=
OAUTH_GOOGLE_CLIENT_SECRET=
